@model Vehicle
@{ 

}

<div class="row">
    <div class="col-12 col-md-6">
        <img src="data:image/jpeg;base64, @Convert.ToBase64String(Model.Photo)" class="img-fluid" />
    </div>
    <div class="col-12 col-md-6">
        <div class="display-4">@Model.Model.Make.Name @Model.Model.Name </div>
        <div class="display-6 text-success">@Model.Price.ToString("n0")</div>
        <div class="text-secondary fs-4">@Model.Km.ToString("n0") km</div>
        <div class="small">@Html.Raw(Model.Description)</div> <!--html olan açıklamaları normal gösterir.-->
    </div> 
    <div class="d-flex gap-2">
        <a href="tel:905305444525" class="btn btn-outline-primary">Ara</a>
        <a href="https://wa.me/15551234567" class="btn btn-outline-primary">Mesaj At</a>
    </div>
    <div>

    </div>
    
</div>